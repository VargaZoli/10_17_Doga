<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet"/>

  <title>SzállásFoglalás</title>
</head>
<body>



    <h1>Szállás foglalás</h1>

    <div class="form-group">




    </div> <form method="post">

      <label for="name">Név</label>
      <input  type="text" id="name" name="name" value="<%= data.name || '' %>" >
    
      <label for="email">Email cím:</label>
      <input type="text " id="email" name="email" value="<% data.email || '' %>" >
    
      <label for="date">Dátum</label>
      <input type="datetime-local" id="date" name="date" value="<% data.date || '' %>" required >
    
      <label for="guests">Vendégek</label>
      <input type="number" id="guests" name="guests" min="1" max="10" value="<% data.guests  %>" >
    
  
    
     <% if (errors && errors.length > 0) { %>
          <div class="alert alert-danger" role="alert">
            <% errors.forEach(function(e) { %>
              <p><%= e %></p>
            <% }); %>
          </div>
          <% } %>
          <button type="submit" class="btn-danger">Foglalás</button>

    </form>
  
   
</body>
</html>
